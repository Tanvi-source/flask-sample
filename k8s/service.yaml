apiVersion: v1
kind: Service
metadata:
  name: flask-app-lb
  namespace: flask-app
spec:
  selector:
    app: flask-app
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
      name: http
  type: LoadBalancer
